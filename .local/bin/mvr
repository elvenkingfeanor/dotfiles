#!/bin/sh
#mvr
# rsync mv

rsync \
    --archive \
    --checksum \
    -hh \
    --partial \
    --progress \
    --info=stats1 \
    --recursive \
    --modify-window=1 \
    --remove-source-files "$@"
