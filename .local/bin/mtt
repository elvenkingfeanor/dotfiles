#!/bin/sh
#mtt
# move selected files/directories
# old script, rarely used
# maybe used with dpath script

/usr/bin/find . \
    -type "$1" \
    -iname "$2" | \
    /usr/bin/sed 's@^\.\/@@g' | \
    /usr/bin/xargs /usr/bin/mv -v -t "$3"
