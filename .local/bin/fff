#!/bin/sh
#fff
# if bootable media is mounted, find its directory structure -quietly

loaded=$(findmnt -rn -S LABEL=backup)

if [ -n "$loaded" ]; then
	notify-send "Generating directory tree"
	find /media/backup/ . > /dev/null 2> /dev/null
	notify-send "Directory tree generated"
fi
