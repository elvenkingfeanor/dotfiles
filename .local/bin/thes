#!/bin/sh
#thes
# Command line thesaurus

BROWSER="lynx -source"
WEBSITE="http://www.thesaurus.com:80/browse/$1"
HTML2TEXT="html2text"

if test $1; then
	${BROWSER} ${WEBSITE} | ${HTML2TEXT} | ${PAGER}
	else
	echo "Usage: $0 word"
	exit 1
fi
