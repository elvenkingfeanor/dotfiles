#!/bin/sh
#itemstring
# find official names (itemstrings) in luanti

find \
    "$MINETEST_USER_PATH" \
    -type f \
    -name "*.lua" \
    -exec grep \
    -rE '\.register_.*\("[_a-z:]+"' "{}" \; | \
    grep "$@"
