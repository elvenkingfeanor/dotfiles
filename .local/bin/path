#!/bin/sh
#path
# yank path of file to system clipboard

/usr/bin/find "$HOME" -path "$HOME"/mdi/ani -prune -false -o -type f -name '*' -print | /usr/bin/fzf -m | /usr/bin/paste -s -d ' ' | /usr/bin/tr -d '\n' | /usr/bin/xclip -selection clipboard
