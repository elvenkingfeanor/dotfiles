#!/bin/sh
#dwmstart
# restart dwm without closing applications
# (terminate X already mapped to Ctrl-Alt-Bksp via /etc/X11/xorg.conf.d/00-keyboard.conf )

while true; do
	# log stderror to file
	dwm 2>> ~/.local/share/dwm.log
done
