#!/bin/sh
#grubi
# install grub using system default settings and generate grub.cfg config file
# old script

doas grub-install \
    --target=x86_64-efi \
    --efi-directory=/boot \
    --bootloader-id=grub-uefi \
    --recheck && \
    doas grub-mkconfig \
    -o /boot/grub/grub.cfg
